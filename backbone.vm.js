// Backbone plugin


var $        = require("jquery");
var Backbone = require("backbone");

var VM = {
  extend: function() {

  }
};

Backbone.VM = VM;
window.Backbone = Backbone;

module.exports = Backbone;
